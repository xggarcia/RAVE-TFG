# All dependencies EXCEPT acids-rave and acids-msprior.
# Those are installed first with --no-deps to avoid the scipy==1.10.0 conflict.
# PyTorch + torchaudio come from the CUDA 12.1 index.

# --- PyTorch (CUDA 12.1) ---
torch>=2.4.0
torchaudio>=2.4.0

# --- Desktop UI ---
PySide6>=6.7
pyqtgraph>=0.13

# --- Audio I/O and processing ---
sounddevice>=0.4
soundfile>=0.12
librosa>=0.10
pyloudnorm>=0.1

# --- Numerics ---
numpy>=1.24
scipy>=1.12
scikit-learn
pandas>=2.0
joblib>=1.3

# --- Training utilities ---
pytorch-lightning>=1.9,<2.0
cached-conv>=2.4.1
einops>=0.5
nn-tilde>=1.4
udls>=1.0

# --- Config / logging ---
gin-config
absl-py>=1.2
tensorboard
tqdm
PyYAML>=6.0
GPUtil>=1.4

# --- Data / network ---
lmdb
pathos
msgpack>=1.0
requests>=2.28
Flask
