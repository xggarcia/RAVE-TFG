# ============================================
# RAVE-TFG Dependencies
# ============================================
# NOTE: acids-rave and acids-msprior are installed
# separately via install.bat / install.sh because
# they pin scipy==1.10.0 which is incompatible with
# Python >=3.12. We install them with --no-deps and
# provide the transitive dependencies here instead.
# ============================================

# --- Core (directly used by this project) ---
# NOTE: torch and torchaudio are installed separately by the installer
# (install.bat / install.sh) so that the CUDA-enabled wheels are picked
# up when an NVIDIA GPU is present. Do not add them here or pip will
# replace the CUDA build with a CPU-only one.
ipython>=8.0.0
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
requests>=2.28.0
sounddevice>=0.4.6
customtkinter>=5.2.0

# --- Transitive deps for acids-rave / acids-msprior ---
# (with flexible versions to support Python >=3.12)
scipy>=1.12.0
# acids-rave still uses the pre-2.0 Lightning API (validation_epoch_end).
# Pin to the 1.9.x series until upstream migrates to on_validation_epoch_end.
pytorch-lightning>=1.9.0,<2.0
absl-py>=1.2.0
cached-conv>=2.4.1
einops>=0.5.0
Flask
gin-config
GPUtil>=1.4.0
nn-tilde>=1.4.0
PyYAML>=6.0
scikit-learn
tensorboard
tqdm
udls>=1.0.0
lmdb
pathos
